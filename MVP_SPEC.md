# 阶段一 MVP 规格说明

## 1. 定位

阶段一的目标不是做一个通用远程 agent 平台，而是先做一个高频、闭环、真实可用的语言训练系统。

系统定位：

- Telegram 作为轻量交互入口
- 本地 agent 负责出题、评估、纠错、总结
- Obsidian 作为长期记忆与训练沉淀层

阶段一优先服务的场景：

- 不带电脑时，通过 Telegram 做日语或英语输出训练
- 通勤、课间、碎片时间进行一轮完整练习
- 训练结束后把结果结构化写入 Obsidian

## 2. 阶段一目标

### 必须实现

- 支持 `/ja` 和 `/en` 开始一轮训练
- 支持用户在训练中直接自然语言作答
- 支持 1 段材料 + 3 道题的单轮训练结构
- 每题答完立即反馈
- 全部结束后生成统一总结
- 总结先发给用户 review，再决定是否写入 Obsidian
- 能提示并恢复上一次未完成训练
- 将训练结果写入固定语言学习目录

### 本阶段重点

- 低摩擦交互
- 状态清晰
- 输出训练有效
- 持续积累长期记忆

### 本阶段不做

- 不做语音识别
- 不做听力音频训练
- 不做多用户
- 不做复杂权限系统
- 不做全 vault 自由编辑
- 不强依赖 Obsidian CLI
- 不强依赖长期 agent session
- 不做多 workflow 通用平台

## 3. 用户画像与训练目标

### 用户画像

- 主要通过 Telegram 使用系统
- 不带电脑时有训练需求
- 日语和英语都练，但当前重点是日语
- 核心需求是输出训练，不是输入刷题
- 接受 agent 响应较慢，但不能状态混乱

### 当前训练目标

- 提升输出能力
- 发现并纠正语法错误
- 优化词语和短语的自然度
- 获得更自然的表达方式
- 建立长期可追踪的训练记录

## 4. 交互原则

### 设计原则

- 少命令，多自然语言
- 保留足够的显式状态提示，避免混乱
- 训练以单轮闭环为主，不依赖长上下文
- 每轮结束时进行可审核的知识沉淀

### 命令策略

阶段一只保留少量命令：

- `/ja`：开始一轮日语训练
- `/en`：开始一轮英语训练
- `/summary`：查看当前训练总结草稿
- `/end`：结束当前训练并进入总结确认阶段

训练开始后，用户直接发送自然语言消息，默认视为当前题目的回答。

## 5. 单轮训练结构

### 标准结构

每轮训练固定为：

- 1 段材料
- 3 道题
- 每题答完立即反馈
- 全部结束后统一总结

### 优先题型

阶段一只做两类题型：

- 中文 -> 目标语言翻译
- 阅读或材料复述

### 训练节奏

推荐流程：

1. 用户发送 `/ja` 或 `/en`
2. 系统检测是否有未完成训练
3. 如有未完成训练，提示是否恢复
4. 如无，agent 生成本轮材料与三道题
5. bot 一次发出本轮训练总览和第 1 题
6. 用户直接作答
7. agent 返回本题反馈
8. bot 推进到下一题
9. 三题完成后生成统一总结
10. 用户 review 总结草稿
11. 用户确认是否写入 Obsidian

## 6. 消息展示格式

### 训练开始消息

每轮开始时，bot 应展示：

- 当前模式：`JA Lesson` 或 `EN Lesson`
- 本轮主题
- 本轮结构：共 3 题
- 当前题目：`1/3`
- 当前任务提示

示例：

```text
[JA Lesson]
主题：课堂沟通
本轮共 3 题
当前题目：1/3

请把下面中文翻译成日语：
今天上课的时候，有一个学生一直不敢开口。
所以我先让他跟着我重复，再让他自己说一遍。
```

### 每题反馈格式

每题反馈固定为 4 段：

1. 你的回答评估
2. 具体问题
3. 更自然表达
4. 替代表达

解释使用中文，目标语言内容保留原文。

示例结构：

```text
[JA Lesson]
当前题目：1/3

1. 你的回答评估
整体意思可以理解，但表达偏直译。

2. 具体问题
- 「开口」直译不自然
- 因果连接略生硬

3. 更自然表达
授業中、ある学生はなかなか発言できませんでした。
そこで、まず私の後について言わせてから、自分の言葉で言ってもらいました。

4. 替代表达
- なかなか声を出せませんでした
- まずはリピートしてもらってから
```

### 总结消息结构

总结固定包含：

- 本次主题
- 做得好的地方
- 主要错误
- 更自然表达精选
- 建议复习点
- 本次总体评价

## 7. 内容长度约束

### 材料长度

- 单段材料建议为 3 到 6 句
- 参考答案可适当扩展到 5 到 6 句
- 不把长度写死在代码中，而通过 prompt 约束

### 回复长度

- 单题反馈允许偏详细
- 但必须控制在 Telegram 友好阅读范围内
- 避免一次性返回过长讲义式内容

## 8. 状态模型

阶段一采用弱状态模型，而不是完全无状态或重度长 session。

### 状态类型

- `idle`：空闲，无当前训练
- `in_lesson`：训练进行中
- `awaiting_summary_confirmation`：等待用户确认总结是否写入
- `interrupted`：训练中断，可恢复

### 当前训练状态字段

每轮训练至少记录：

- `lesson_id`
- `language`：`ja` 或 `en`
- `topic`
- `material`
- `questions`
- `current_question_index`
- `user_answers`
- `feedbacks`
- `draft_summary`
- `status`
- `created_at`
- `updated_at`

### 状态切换

- `/ja` 或 `/en`：`idle -> in_lesson`
- 用户答题：保持 `in_lesson`
- 三题完成或 `/end`：`in_lesson -> awaiting_summary_confirmation`
- 用户确认写入：`awaiting_summary_confirmation -> idle`
- 训练中断：标记为 `interrupted`
- 下次启动时可 `interrupted -> in_lesson`

## 9. 中断与恢复

### 恢复策略

如果存在未完成训练，下次用户发送 `/ja` 或 `/en` 时，先提示：

```text
你有一轮未完成的训练。
主题：课堂沟通
当前进度：2/3

回复：
- 恢复上次训练
- 放弃并开始新的训练
```

### 阶段一策略

- 不自动恢复
- 必须用户明确选择
- 避免用户忘记上下文时被强行带回旧题

## 10. Obsidian 集成策略

### 长期记忆原则

阶段一的长期记忆优先依赖 Obsidian，而不是 agent 的长期上下文。

原因：

- 实现简单
- 稳定可控
- 容易审阅
- 后续易迁移到通用平台

### 写入原则

- 训练结束后先生成总结草稿
- 用户 review 后决定是否写入
- 阶段一直接写 Markdown 文件
- 不强依赖 Obsidian CLI

### 建议目录结构

```text
Language/
├─ Journal/
│  ├─ 2026/
│  │  ├─ 2026-03-11-ja.md
│  │  └─ 2026-03-11-en.md
├─ Japanese/
│  ├─ Mistakes/
│  └─ Expressions/
├─ English/
│  ├─ Mistakes/
│  └─ Expressions/
└─ Profile/
   ├─ language-profile.md
   └─ current-goals.md
```

### 三类产物

#### 1. 每日训练日志

按日期生成一篇，记录：

- 训练时间
- 语言
- 主题
- 材料
- 用户回答
- 每题反馈摘要
- 本轮总结

#### 2. 错题本

按错误类型聚合，不按日期散落。

例如：

- 助词问题
- 时态问题
- 搭配不自然
- 语气问题
- 句式单一
- 中式或英式直译

#### 3. 表达库

只记录高价值内容：

- 更自然表达
- 替代表达
- 用户反复不会说的意思

### 写入前确认

训练结束后，bot 先发送总结草稿，并提示：

- `确认写入`
- `重写总结`
- `不写入`

阶段一只在用户明确确认后写入 Obsidian。

## 11. Agent 责任边界

### agent 负责

- 生成材料
- 生成题目
- 评估用户回答
- 给出错误分析
- 给出更自然表达
- 给出替代表达
- 生成本轮总结
- 提炼错题与表达积累

### 本地程序负责

- Telegram 命令路由
- 状态管理
- 会话恢复判断
- Obsidian 文件写入
- 消息格式包装
- 超时与错误处理
- 日志与审计

## 12. Prompt 策略

阶段一应尽量通过 prompt 控制训练质量，而不是过早写死太多业务逻辑。

### 系统级约束

对 agent 的核心要求：

- 你是一名语言训练教练
- 训练目标是提升用户输出能力
- 优先纠正语法错误与不自然表达
- 解释使用中文
- 日语或英语内容使用原文
- 优先生成适合教学、生活、基础表达场景的材料
- 避免过于职业化、词汇负担过重的材料
- 每轮固定 1 段材料和 3 道题
- 每题反馈固定为 4 个部分

### 日语方向附加约束

- 优先自然表达
- 难度在中高级过渡区间
- 避免纯教材腔
- 避免过度依赖专业词汇

### 英语方向附加约束

- 优先自然写作表达
- 重点看语法、搭配、自然度
- 避免考试技巧导向

## 13. 评价体系

阶段一先做单次评价，不先做复杂的长期趋势算法。

### 每轮评价维度

- 准确度
- 自然度
- 表达丰富度
- 稳定性

### 展示方式

在本轮总结里用简短语言说明：

- 哪方面表现较好
- 哪方面最需要加强
- 下次训练建议重点

长期趋势统计可以留到阶段二实现。

## 14. 非功能要求

### 响应要求

- 接受 agent 响应较慢
- 但 bot 不能无提示卡死
- 需要有明确的当前状态展示

### 稳定性要求

- 训练中状态不可混乱
- 不应误把无关消息当作别的任务
- 中断后要能明确恢复或丢弃

### 可扩展性要求

阶段一的实现要为后续升级成更大的远程 agent 平台留出空间。

因此建议底层结构预留：

- Telegram 接入层
- Lesson Workflow 层
- Agent Adapter 层
- Obsidian Memory 层
- Session State 层

## 15. 非目标边界

阶段一明确不包含：

- 语音输入
- 自动语音评分
- 听力材料播放
- 自由多任务切换
- 全平台型 `/run` 通用指令
- 通用远程终端功能
- 多 agent 自动编排
- 全 Obsidian vault 自动编辑

## 16. 成功标准

满足以下条件即可认为阶段一 MVP 达标：

1. 用户可以通过 `/ja` 或 `/en` 稳定开启一轮训练
2. 一轮训练能按 1 段材料 + 3 题完整跑完
3. 用户可以直接自然语言作答
4. 每题反馈结构稳定且可读
5. 总结能先 preview 再确认写入
6. Obsidian 能稳定生成训练日志、错题沉淀和表达积累
7. 中断训练时，下一次可提示恢复
8. 训练过程不会产生明显状态混乱

## 17. 下一阶段接口预留

阶段一实现时，建议预留未来扩展点：

- `workflow = language_tutor`
- `memory_backend = obsidian`
- `agent_backend = codex | opencode`
- `session_policy = stateless | resumable`

这样后续从“小圆”扩展到“大圆”时，不需要重写核心结构。

## 18. 一句话总结

阶段一要做的是一个低摩擦、状态清晰、以输出训练为核心、训练后可沉淀到 Obsidian 的 Telegram 语言训练系统，而不是一个功能很全但体验发散的通用 agent 平台。
