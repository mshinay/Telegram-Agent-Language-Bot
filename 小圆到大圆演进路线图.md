# 小圆 -> 大圆 演进路线图

## 核心思路

先做一个高频、闭环、真实有价值的小圆：

- Telegram 入口
- 单用户
- 调用本地 agent
- 读取/写入 Obsidian
- 完成语言学习闭环

再在这个基础上逐步向外扩展，最终形成个人远程 agent 平台。

这不是先做一个临时玩具，再推翻重来。

而是：

**先把一个高频小圆做深，再把共性能力抽出来，扩成大圆。**

---

## 阶段 0：最小链路验证

### 目标

验证：

- Telegram -> 本地 bot -> agent -> Telegram 这条链路是否可用
- 延迟是否可接受
- 主要瓶颈在 Telegram 还是 agent

### 做这些

- 单用户
- 单任务锁
- 白名单路径
- `/ping`
- `/run <project> | <prompt>`
- 固定 agent
- 分段耗时日志

### 验收标准

- `/ping` 稳定
- `/run` 能稳定触发 `codex` 或内置任务
- 能区分 Telegram 延迟和 agent 延迟

---

## 阶段 1：小圆，语言学习工具

### 目标

做成你自己每天愿意用的日语/英语教练。

### 做这些

- Telegram 命令改成场景化
- `/ja` 开始日语训练
- `/en` 开始英语训练
- `/answer <内容>` 提交答案
- `/summary` 生成当次总结
- 读取 Obsidian 中固定目录的学习背景
- 训练后把结果写回 Obsidian 每日笔记
- 保存最近一轮会话状态

### Obsidian 固定目录

- `Language/Journal/`
- `Language/Japanese/Mistakes/`
- `Language/English/Mistakes/`
- `Language/Patterns/`
- `Language/Profile/`

### 验收标准

- 能完成一轮“出题 -> 回答 -> 纠错 -> 写笔记”
- 日语和英语至少各有一个模板
- 笔记结构稳定，不乱写 vault

---

## 阶段 2：中圆，个人学习与知识助手

### 目标

不只是出题，而是开始复用长期记忆。

### 做这些

- `/review` 复习最近错题
- `/today` 查看今日训练摘要
- `/week` 汇总最近 7 天问题
- 根据 Obsidian 里的旧错题和主题笔记生成下一轮题目
- 支持按主题训练
- `/ja business`
- `/ja listening`
- `/en speaking`

### 这一阶段开始抽象

- `workflow: language_tutor`
- `memory: obsidian`
- `session: current_lesson`

### 验收标准

- 训练题目能利用历史笔记
- 能看到“你最近总错什么”
- 你会开始依赖它做日常复习

---

## 阶段 3：大圆的骨架，通用远程 agent 平台

### 目标

把语言学习系统底层抽象成可扩展平台。

### 底层模块固定下来

- Telegram 接入层
- Command Router
- Agent Adapter
- Session Store
- Tool Layer
- Memory Layer
- Workflow Layer
- Audit Log

### 这时增加通用能力

- `/run`
- `/status`
- `/stop`
- agent 切换：`codex` / `opencode`
- 项目白名单
- 任务级 session
- 更清晰的错误与状态回报

### 验收标准

- 语言工作流不需要重写
- 新增 workflow 时只接适配层，不改核心框架
- `codex` 和 `opencode` 至少支持一个统一接口

---

## 阶段 4：大圆，个人远程 agent 平台

### 目标

语言学习只是其中一个 workflow，其它个人场景也能接进来。

### 新增 workflow

- `obsidian_assistant`
- 总结最近笔记
- 整理草稿为正式笔记
- 生成周报/月报
- `code_helper`
- 只读分析项目
- 查重构建议
- 总结目录/模块
- `research_assistant`
- 归纳网页、PDF、资料
- 输出学习摘要

### 新增平台能力

- 多 session
- 恢复历史任务
- 附件处理
- 更长输出分段发送
- 权限分级
- 任务队列

### 验收标准

- Telegram 已经成为你的统一远程入口
- Obsidian 成为长期记忆层
- agent 成为具体能力执行层

---

## 始终不变的原则

- 先做高频闭环，不先做大全
- 工作流优先，平台抽象随后固化
- 长期记忆优先落在 Obsidian，不只依赖 agent session
- 所有写操作只允许固定目录和固定模板
- 能用日志判断慢在哪，不靠感觉

---

## 当前最推荐的下一步

直接进入阶段 1，做语言学习版 MVP：

- `/ja`
- `/en`
- `/answer`
- `/summary`
- 写入 Obsidian 每日训练记录

---

## 一句话总结

先把语言学习这条高频闭环做成一个稳定可用的小圆，再把底层共性能力抽出来，逐步扩展成个人远程 agent 平台。
